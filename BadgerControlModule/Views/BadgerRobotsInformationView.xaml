<UserControl x:Class="BadgerControlModule.Views.BadgerRobotsInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300"
             Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" ShowGridLines="True">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            
            <Label Content="Robot's Information" HorizontalAlignment="Left" VerticalAlignment="Top" Height="26" Width="119" Grid.Row="0" Grid.Column="0"/>
            <ComboBox HorizontalAlignment="Center"  Width="152" Height="22" Grid.Row="0" Grid.Column="0" Margin="0,25,0,0" VerticalAlignment="Top" ItemsSource="{Binding SubsystemList}" SelectedItem="{Binding SelectedSubsystem}"/>
         </Grid>
        <Grid Grid.Column="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            
            <TreeView Name="TreeView1" HorizontalAlignment="Left" VerticalAlignment="Top" Width ="120" Grid.Row="0" Grid.Column="1">
                <TreeViewItem Header="Header">
                    <TreeViewItem Header="Subheader 1">
                    </TreeViewItem>
                    <TreeViewItem Header="Subheader 2">
                    </TreeViewItem>
                    <TreeViewItem Header="Subheader 3">
                    </TreeViewItem>
                </TreeViewItem>
            </TreeView>
        </Grid>

        <!-- put shit into the grids to make everything neat and resizeable -->
    </Grid>
</UserControl>
