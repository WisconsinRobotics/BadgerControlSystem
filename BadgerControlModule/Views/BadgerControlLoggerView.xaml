<UserControl x:Class="BadgerControlModule.Views.BadgerControlLoggerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="350"
             Height="auto"
             Width="auto">
    <Grid x:Name="LoggerGrid" Height="400"> 
        <TextBox x:Name="LoggerTextBox" TextWrapping="Wrap" 
            Background="LightGray" IsReadOnly="True" Height="Auto"
            Text="{Binding Path=LogText}"
            VerticalScrollBarVisibility="Visible"  Margin="5"/>
    </Grid>
</UserControl>
